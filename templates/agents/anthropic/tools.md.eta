---json
{ "provider": "anthropic" }
---

# Tools (Anthropic)

<% if (cfg && cfg.tools) { %>
<% for (const t of cfg.tools) { %>
- <%= t.name %>: <%= t.description || '' %>
<% } %>
<% } else { %>
No tools configured.
<% } %>
